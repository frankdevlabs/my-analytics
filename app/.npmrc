# NPM Configuration
# Use legacy peer dependency resolution for Next.js 16 compatibility
# This matches the local development environment and resolves conflicts with
# beta packages (e.g., next-auth) that haven't updated their peer dependencies yet
legacy-peer-deps=true
