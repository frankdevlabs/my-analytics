/**
 * Gatsby Configuration
 * @type {import('gatsby').GatsbyConfig}
 */
module.exports = {
  siteMetadata: {
    title: `My Analytics Demo Site`,
    description: `A demonstration website showcasing My Analytics tracking capabilities including pageviews, custom events, UTM parameters, and multi-page session tracking.`,
    siteUrl: `http://localhost:8000`,
    author: `My Analytics Team`
  },
  plugins: [
    // No plugins required for minimal setup
    // Plugins will be added as needed for future features
  ]
}
